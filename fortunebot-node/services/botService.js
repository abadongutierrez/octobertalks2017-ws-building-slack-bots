var superagent = require('superagent');
var localDbService = require('../services/localDbService');

module.exports = {
	auth: function(code, callback) {
		// TODO: call GET "https://slack.com/api/oauth.access"
		// TIP: use superagent.get
		// TIP: Use the callback argument to attend superagent result
    },    
};